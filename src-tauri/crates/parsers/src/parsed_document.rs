//! `ParsedDocument` + `ParseOptions` live in `specdex_core::parse`; re-exported
//! here so impl modules and downstream callers keep their existing paths.

pub use specdex_core::parse::{ParseOptions, ParsedDocument};
