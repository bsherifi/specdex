//! Specdex Tauri adapter — binary entry point.
//!
//! In plan 01 this is a placeholder that prints a marker so the workspace
//! has a runnable binary for CI. Plan 02 replaces this with the real Tauri
//! bootstrap (window, menus, command handlers). See SPECDEX-V1.md §9.4.

fn main() {
    println!("specdex placeholder (plan 01)");
}
