# Screenshots

- `viewer.png` — PdfViewer with 3 highlighted codes (light mode).
- `kbs.png` — KbList with 3 example KBs.
- `entry.png` — entry editor with aliases panel + source back-reference.

Captured by `e2e/screenshots.spec.ts` (plan 05 baseline; flow-specific
screenshots can be added with `await driver.takeScreenshot()`).
