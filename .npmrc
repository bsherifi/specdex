# Reproducible installs; never auto-upgrade engines on install.
engine-strict=true
# pnpm flat node_modules for Tauri-friendly imports.
shamefully-hoist=true
# Don't auto-install peer deps; force explicit declarations.
auto-install-peers=false
