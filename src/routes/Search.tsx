import type { JSX } from "react";
import { EmptyState } from "@/components/shared";

export default function Search(): JSX.Element {
  return (
    <EmptyState
      title="Search"
      description="This route is a placeholder. Plan 22 fills it in."
    />
  );
}
