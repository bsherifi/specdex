import type { JSX } from "react";
import { EmptyState } from "@/components/shared";

export default function Documents(): JSX.Element {
  return (
    <EmptyState
      title="Documents"
      description="This route is a placeholder. Plan 23 fills it in."
    />
  );
}
