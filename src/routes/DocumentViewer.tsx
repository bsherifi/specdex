import type { JSX } from "react";
import { EmptyState } from "@/components/shared";

export default function DocumentViewer(): JSX.Element {
  return (
    <EmptyState
      title="Document viewer"
      description="This route is a placeholder. Plan 24 fills it in."
    />
  );
}
