import type { JSX } from "react";
import { EmptyState } from "@/components/shared";

export default function Settings(): JSX.Element {
  return (
    <EmptyState
      title="Settings"
      description="This route is a placeholder. Plan 30 fills it in."
    />
  );
}
