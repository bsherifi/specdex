import type { JSX } from "react";
import { EmptyState } from "@/components/shared";

export default function Onboarding(): JSX.Element {
  return (
    <EmptyState
      title="Onboarding"
      description="This route is a placeholder. Plan 29 fills it in."
    />
  );
}
